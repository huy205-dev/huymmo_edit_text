export default defineAppConfig({
  ui: {
    colors: {
      primary: 'green',
      neutral: 'zinc'
    },
    button: {
      defaultVariants: {
        size: 'sm',
        color: 'primary'
      }
    }
  }
})
